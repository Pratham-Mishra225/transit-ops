/**
 * Re-exports Prisma-generated Vehicle types for use across UI components,
 * keeping UI layers decoupled from direct @prisma/client imports.
 */
export type { Vehicle, VehicleStatus, VehicleType } from "@prisma/client";
